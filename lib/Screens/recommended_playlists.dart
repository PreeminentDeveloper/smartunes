class RecommendedPlaylists{

  final int id;
  String title;
  String preview;
  String cover;
  String albumTitle;
  
  RecommendedPlaylists(this.id, this.title, this.preview, this.cover, this.albumTitle);
}